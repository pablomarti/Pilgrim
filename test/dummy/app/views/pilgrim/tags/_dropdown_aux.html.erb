<%#= select form_name, name, data.collect{ |i| [i.name, i.id] }, :class => html_class, :id => html_id, :data => { kind: kind, block: block, name: name, html_class: html_class, formname: form_name } %>

<%= select form_name, name, options_for_select(data.collect{ |c| [c.name, c.id] }, 1), {}, :data => {:kind => kind, :block => block, :formname => form_name, :name => name}, :class => html_class, :id => html_id %>
